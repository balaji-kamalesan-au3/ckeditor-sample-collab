/**
 * @license Copyright (c) 2003-2022, CKSource Holding sp. z o.o. All rights reserved.
 */
import * as Core from '@ckbox/core';
export { version } from '@ckbox/core';
import * as react from 'react';
export { react as React };

declare type Asset = Core.Asset;
declare type CoreDialogProps = Required<Core.Props>['dialog'];
declare type CoreAssetsProps = Core.Props['assets'];
declare type DialogProps = Omit<Exclude<CoreDialogProps, boolean>, 'open'>;
declare type Props = Omit<Core.Props, 'dialog'> & {
    dialog?: boolean | DialogProps;
};

/**
 * Creates an instance and mounts it at supplied node.
 *
 * @param root instance's root element
 * @param props instance's config options
 */
declare const mount: (root: Element, props: Props) => {
    unmount: () => void;
};

export { Asset, CoreAssetsProps, CoreDialogProps, DialogProps, Props, mount };
